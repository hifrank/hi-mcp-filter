# Implementation Plan: MCP SSE Transport Support

**Branch**: `002-mcp-sse-transport` | **Date**: 2026-01-27 | **Spec**: [spec.md](./spec.md)  
**Input**: Feature specification from `/specs/002-mcp-sse-transport/spec.md`

**Note**: This plan was generated by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

Add Server-Sent Events (SSE) transport support to MCP proxy, enabling proxying of Azure API Management MCP endpoints and other SSE-based MCP servers. Implement transport detection (HTTP vs SSE), SSE stream parsing, and JSON-RPC message extraction. Maintain backward compatibility with existing HTTP-only configurations.

**Technical Approach**: Use `eventsource-parser` library for SSE parsing, extend `MCPServerConfig` schema with `transport` field ("http"|"sse"|"auto"), implement auto-detection via Content-Type header, buffer SSE events until `close` event, apply existing filters/transforms to extracted JSON-RPC messages.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode), Node.js 20.x  
**Primary Dependencies**: Fastify 5.x, eventsource-parser 1.x, ajv 8.x, JSONata 2.x, isolated-vm 5.x  
**Storage**: N/A (configuration from JSON files only)  
**Testing**: Vitest (47 existing tests), supertest for integration tests  
**Target Platform**: Linux server (Docker), macOS (local dev)  
**Project Type**: Single project (Node.js backend service)  
**Performance Goals**: 
  - SSE parsing latency <10ms per event
  - Support 1000 req/s aggregate throughput (same as feature 001)
  - Timeout accuracy ±5% of configured value
**Constraints**: 
  - Maintain backward compatibility with feature 001 HTTP-only configs
  - SSE buffer limit: 1000 events max to prevent memory exhaustion
  - Request timeout: 1s min, 5min max (existing constraint)
**Scale/Scope**: 
  - Support mixed HTTP and SSE servers (multiple servers in one proxy instance)
  - Handle Azure APIM SSE format: `event: message\ndata: {JSON-RPC}\n\nevent: close\ndata:\n\n`
  - ~500 LOC new code (SSE parser, transport detection, config schema extension)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### ✅ Principle I: Specification-Driven Development

- [x] Feature spec exists: [spec.md](./spec.md) (273 lines, 4 user stories, 11 FRs, 6 success criteria)
- [x] Acceptance criteria defined: FR-010 (all existing tests pass), FR-011 (no config changes required for HTTP)
- [x] Compliance: Full compliance

### ✅ Principle II: Test-First Discipline

- [x] Test strategy defined: Contract tests (SSE parsing), integration tests (Azure APIM endpoint), unit tests (transport detection)
- [x] Test cases specified: FR-009 (SSE parsing, timeout, error handling, auto-detection)
- [x] Compliance: Full compliance (tests required for all FRs)

### ✅ Principle III: Container-First Deployment

- [x] Container impact: No changes required (existing Dockerfile supports new dependencies)
- [x] Config externalization: `MCPServerConfig.transport` and `sseOptions` in JSON config files
- [x] Compliance: Full compliance (no Dockerfile changes needed)

### ✅ Principle IV: Minimal Abstraction

- [x] Direct library usage: `eventsource-parser` used directly, no custom SSE parser
- [x] Essential state only: SSE connection state (CONNECTING → CONNECTED → CLOSED), event buffer
- [x] Justification: Transport detection requires distinguishing HTTP vs SSE; buffering required by SSE protocol
- [x] Compliance: Full compliance (no unnecessary abstractions)

### ✅ Principle V: Explicit Configuration

- [x] Runtime config: `transport` field in `MCPServerConfig` (explicit "http"|"sse"|"auto")
- [x] No magic defaults beyond standard practice: `transport: "auto"` for backward compatibility
- [x] Compliance: Full compliance (explicit transport configuration)

### ✅ Principle VI: Fail-Fast Error Handling

- [x] Early validation: Config schema validation on startup (ajv), SSE format validation on first event
- [x] Clear error messages: "Backend SSE stream error: Invalid JSON in data field", "SSE stream timeout after Xms"
- [x] Compliance: Full compliance (502 on parse error, 504 on timeout)

### ✅ Principle VII: Observable Behavior

- [x] Logging strategy: SSE connection lifecycle (connect, first event, close, error), transport detection
- [x] Metrics exposure: `sse_events_parsed_total`, `sse_parse_errors_total`, `sse_timeouts_total` (Prometheus)
- [x] Health indicators: Existing `/health` and `/metrics` endpoints expose SSE metrics
- [x] Compliance: Full compliance (SSE-specific metrics added)

**GATE RESULT: ✅ PASS** - All 7 principles satisfied, no violations requiring justification.

## Project Structure

### Documentation (this feature)

```text
specs/002-mcp-sse-transport/
├── spec.md                              # Feature specification (user stories, FRs)
├── checklists/
│   └── requirements.md                  # Spec quality checklist (approved)
├── plan.md                              # This file (/speckit.plan output)
├── research.md                          # Phase 0 output (technical decisions)
├── data-model.md                        # Phase 1 output (entity definitions)
├── quickstart.md                        # Phase 1 output (usage guide)
└── contracts/                           # Phase 1 output (API contracts)
    ├── README.md                        # Contracts index
    ├── sse-proxy-endpoint.md            # SSE-enabled /proxy/:serverId endpoint
    └── transport-config.md              # Transport configuration schema
```

### Source Code (repository root)

```text
src/
├── index.ts                             # Entry point (route registration - MODIFY)
├── proxy/
│   ├── routes/
│   │   └── proxy.ts                     # Proxy routes (transport detection - MODIFY)
│   ├── forwarder.ts                     # Backend forwarder (SSE support - MODIFY)
│   ├── sse-parser.ts                    # SSE event parser (NEW)
│   └── transport.ts                     # Transport detection logic (NEW)
├── config/
│   ├── schema.ts                        # Config schema (extend MCPServerConfig - MODIFY)
│   └── validator.ts                     # Config validation (transport validation - MODIFY)
└── types/
    ├── config.ts                        # Type definitions (TransportType, SSETransportConfig - MODIFY)
    └── sse.ts                           # SSE-specific types (SSEEvent, SSEConnection - NEW)

tests/
├── unit/
│   ├── sse-parser.test.ts               # SSE parser unit tests (NEW)
│   ├── transport.test.ts                # Transport detection tests (NEW)
│   └── config/
│       └── validator.test.ts            # Config validation tests (MODIFY)
├── integration/
│   ├── sse-proxy.test.ts                # SSE endpoint integration tests (NEW)
│   └── azure-apim.test.ts               # Azure APIM endpoint test (NEW)
└── contract/
    └── sse-format.test.ts               # SSE format contract test (NEW)

config/
├── default.json                         # Default config (add SSE example - MODIFY)
└── azure-mcp.json                       # Azure APIM config (set transport: "sse" - MODIFY)

docs/
└── sse-transport.md                     # SSE transport documentation (NEW)
```

**Structure Decision**: Single project structure (Node.js backend service). SSE support is implemented as an extension to existing proxy forwarder logic. New files: `sse-parser.ts`, `transport.ts`, `types/sse.ts` (isolated components). Modified files: `forwarder.ts`, `schema.ts`, `proxy.ts` (minimal integration points). Test structure mirrors source structure (unit/integration/contract).

## Complexity Tracking

> **No complexity violations** - All Constitution principles satisfied.

N/A - This section is empty because there are no unjustified violations requiring documentation.
