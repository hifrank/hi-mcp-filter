apiVersion: v1
kind: Service
metadata:
  name: mcp-proxy
  namespace: default
  labels:
    app: mcp-proxy
spec:
  type: ClusterIP
  selector:
    app: mcp-proxy
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  sessionAffinity: None
